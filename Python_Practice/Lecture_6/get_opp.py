def main() -> None:
    operation = input('Opperation>> ')
    operators = ('+', '-', '*', '/')
    op_parts = opperation.split()
    for part in op_parts:
        focus = list(set(op_parts).)
        if
